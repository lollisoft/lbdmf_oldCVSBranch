# Standard template

ifeq ($(OSTYPE), Windows_NT)
include $(DEVROOT)\projects\CPP\make\STD_MAKE
endif

ifeq ($(OSTYPE), linux)
include $(DEVROOT)/Projects/CPP/make/STD_MAKE
endif

ifeq ($(OSTYPE), osx)
include $(DEVROOT)/Projects/CPP/make/STD_MAKE
endif

